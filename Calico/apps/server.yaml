apiVersion: v1
kind: Pod
metadata:
  name: web-server
  labels:
    app: calico-vxlan
    role: server

spec:
  nodeName: k01-worker
  containers:
  - name: nginx
    image: nginx:1.25
    ports:
    - containerPort: 80